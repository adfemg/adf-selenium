package com.redheap.selenium.component;

import org.openqa.selenium.WebDriver;

public class AdfPanelDrawer extends AdfComponent {

    public AdfPanelDrawer(WebDriver webDriver, String clientid) {
        super(webDriver, clientid);
    }
}
